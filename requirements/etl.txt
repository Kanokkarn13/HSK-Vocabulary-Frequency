# setuptools<84 pinned because openai-whisper's legacy setup.py needs
# pkg_resources at build time, which newer setuptools dropped by default.
setuptools<84
pandas==3.0.3
# numpy>=2.5.0 requires Python >=3.12; this project (and CI) pins Python 3.11
# everywhere, so pin numpy below that floor. Bump this only alongside a
# Python version bump across backend/etl/CI, not independently.
# A Dependabot PR bumped this to 2.5.1 anyway and got merged despite its own
# CI run failing on exactly this incompatibility -- reverted back down.
numpy==2.4.6
pyarrow==24.0.0
matplotlib==3.11.0
pdfplumber==0.11.4
openai-whisper==20250625
jieba==0.42.1
opencc-python-reimplemented==0.1.7
requests==2.34.2
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.0.1
